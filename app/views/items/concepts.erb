<table>
<% @occurrences.each do |oc| %>
  <tr>
    <td><%= link_to oc.document.title, oc.document, :title=>oc.document.to_s(true) %></td>
    <td><%= oc.weight %></td>
    <td><%= link_to oc.concept.title,  oc.concept %></td>
  </tr>
<% end %>
</table>

<br />
<%= will_paginate(@occurrences)%>
